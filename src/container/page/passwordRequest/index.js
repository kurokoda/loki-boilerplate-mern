import { connect } from 'react-redux';

import { PasswordRequestPage } from '../../../component/page';

export function mapStateToProps() {
  return {};
}

export function mapDispatchToProps(dispatch) {
  return {};
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(PasswordRequestPage);
